<%- include('_admin-header', { pageTitle: 'Anmeldungen: ' + anlass.title, active: 'anlaesse' }) %>

<div class="admin-topbar">
  <h1>Anmeldungen: <%= anlass.title %></h1>
  <div style="display:flex; gap:1rem;">
    <a href="/admin/registrations/juniorenlager/new?anlass_id=<%= anlass.id %>" class="btn btn-primary">+ Neue Anmeldung erfassen</a>
    <a href="/admin/anlaesse" class="btn btn-ghost">Zurück zu Anlässe</a>
  </div>
</div>

<div class="admin-card">
  <div style="overflow-x: auto;">
    <table class="admin-table">
      <thead>
        <tr>
          <th>Kind</th>
          <th>Geburtsdatum</th>
          <th>Elternteil</th>
          <th>E-Mail</th>
          <th>Telefon</th>
          <th>Allergien</th>
          <th>Datum</th>
          <th class="actions">Aktionen</th>
        </tr>
      </thead>
      <tbody>
        <% registrations.forEach(r => { %>
          <tr>
            <td><strong><%= r.child_name %></strong></td>
            <td><%= r.child_birthdate || '-' %></td>
            <td><%= r.parent_name %></td>
            <td><a href="mailto:<%= r.parent_email %>"><%= r.parent_email %></a></td>
            <td><%= r.parent_phone %></td>
            <td><%= r.allergies ? r.allergies.substring(0, 20) + '...' : '-' %></td>
            <td><%= formatDateTime(r.created_at) %></td>
            <td class="actions">
              <a class="btn btn-sm btn-primary" href="/admin/registrations/juniorenlager/<%= r.id %>/edit?anlass_id=<%= anlass.id %>">Bearbeiten</a>
              <form action="/admin/registrations/juniorenlager/<%= r.id %>/delete" method="POST" onsubmit="return confirm('Wirklich löschen?');" style="display:inline;">
                <input type="hidden" name="anlass_id" value="<%= anlass.id %>">
                <button class="btn btn-sm btn-danger" type="submit">Löschen</button>
              </form>
            </td>
          </tr>
        <% }) %>
        <% if (registrations.length === 0) { %>
          <tr>
            <td colspan="8" style="text-align:center; padding:2rem;">Noch keine Anmeldungen.</td>
          </tr>
        <% } %>
      </tbody>
    </table>
  </div>
</div>

<%- include('_admin-footer') %>
